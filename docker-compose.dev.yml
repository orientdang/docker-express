version: "3"
services:
  node-app:
    build:
      context: .
      args:  
        NODE_ENV: development
    ports: 
      - "3000:3000"
    volumes:
      - ./:/app:ro
      - /app/node_modules
    command: npm run dev
    environment:
      - NODE_ENV=production
